---
import Footer from '../components/Base/Footer.astro';
import Header from '../components/Base/Header.astro';
import MainContent from '../components/Base/MainContent.astro';
import Meta from '../components/Base/Meta.astro';
import SkipToContent from '../components/Base/SkipToContent.astro';
import '../css/config.css'
import type { Layout } from './types';

interface Props extends Layout { }

const {
    title,
    description,
	lang = 'en',
	class : className
} = Astro.props
---

<html lang={lang}>

	<Meta title={title} description={description}>
		<slot name="meta" />
	</Meta>

	<body class:list={[
		'bg-[var(--body-bg-color)] text-primary-900 font-base lg:text-lg ',
		'mobile-nav-is-open',
		className
	]}>

		<SkipToContent />

		<Header variant='fixed' props />
		
        <MainContent>

            <slot />

        </MainContent>

        <Footer />

		<slot name="pageEnd" />
		
	</body>
</html>


