

<div 
    class="fixed inset-0 -z-50 bg-primary-50/90 opacity-0 pt-4 transition-all duration-500"
    data-img-modal
/>

<style is:global>
	.img-modal-animation {
		animation: img-modal-animation 300ms ease-in-out;
	}
	
	@keyframes img-modal-animation {
		from { transform : translateY(3rem); }
		to   { transform: translateY(0rem);}
	}
</style>

<script>
import GalleryModal from '../lib/galleryModal'

const modal = new GalleryModal({
    modal  : '[data-img-modal]',
    imgBtn : '[data-img-button]',
})
modal.init()

import.meta.env.DEV && modal.mockOpenModal()

</script>



