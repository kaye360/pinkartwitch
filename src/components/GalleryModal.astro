
<div 
    class="fixed inset-0 -z-50 overflow-y-scroll scrollbar-hide bg-primary-50/95 opacity-0 pt-4 transition-all duration-500"
    data-art-modal
/>

<style is:global>
	.img-modal-animation {
		animation: img-modal-animation 300ms ease-in-out;
	}
	
	@keyframes img-modal-animation {
		from { transform : translateY(3rem); }
		to   { transform: translateY(0rem);}
	}
</style>

<script>
import GalleryModal from '../lib/galleryModal/galleryModal'

const modal = new GalleryModal({
    modal  : '[data-art-modal]',
    imgBtn : '[data-art-button]',
})
modal.init()
// import.meta.env.DEV && modal.mockOpenModal()

</script>



